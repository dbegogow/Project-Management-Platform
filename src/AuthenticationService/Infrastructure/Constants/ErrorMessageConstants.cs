namespace AuthenticationService.Infrastructure.Constants;

public class ErrorMessageConstants
{
    public const string NotExistingRole = "The Role not exist in the system";

    public const string EmailExist = "There is already a user with this email in the system";
}
